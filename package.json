{
  "name": "rpow",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "tsc -b apps/server packages/shared packages/solana-bridge && npm --workspace apps/web run build",
    "test": "npm --workspace packages/shared test && npm --workspace packages/solana-bridge test && npm --workspace apps/server test && npm --workspace apps/web test",
    "lint": "tsc -b --noEmit && eslint .",
    "dev:server": "npm --workspace apps/server run dev",
    "dev:web": "npm --workspace apps/web run dev"
  },
  "engines": { "node": ">=22.0.0" },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^22.0.0"
  }
}
